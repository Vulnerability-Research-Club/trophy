name: Process JSON Data

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'proj/result.db'  # Ignore changes to result.db so it doesn't trigger another run

jobs:
  process-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      - name: Run process_json.py
        run: python .github/process_json.py

      - name: Commit updated DB file
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add proj/result.db
          # Only commit if result.db has changed
          git diff-index --quiet HEAD || git commit -m "Update result.db"

      - name: Push changes
        run: git push
